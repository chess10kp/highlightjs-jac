import:py from random { randint }

node item {
    has value: int = 0;
}

walker traversal {
    has target: int;

    can travel with root entry {
        spawn here ++> item(value=randint(1,10));
        self.target = 5;
        disengage;
    }
}
