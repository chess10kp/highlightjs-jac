<span class="hljs-comment"># del operator in f-string substitution</span>
x = <span class="hljs-string">f&quot;<span class="hljs-subst">{<span class="hljs-keyword">del</span> a}</span>&quot;</span>;

<span class="hljs-comment"># Connect operators in f-string substitution</span>
y = <span class="hljs-string">f&quot;<span class="hljs-subst">{a <span class="hljs-operator">--&gt;</span> b}</span>&quot;</span>;

<span class="hljs-comment"># del with connect in f-string substitution</span>
z = <span class="hljs-string">f&quot;<span class="hljs-subst">{<span class="hljs-keyword">del</span> a <span class="hljs-operator">--&gt;</span> b}</span>&quot;</span>;
