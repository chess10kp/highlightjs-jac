<span class="hljs-comment"># JSX</span>
html = &lt;div&gt;Hello&lt;/div&gt;;

<span class="hljs-comment"># JSX with attributes</span>
html = &lt;div class=<span class="hljs-string">&quot;foo&quot;</span>&gt;Hello&lt;/div&gt;;

<span class="hljs-comment"># JSX with self-closing</span>
html = &lt;img /&gt;;

<span class="hljs-comment"># JSX with fragment</span>
html = &lt;&gt;Hello&lt;/&gt;;

<span class="hljs-comment"># JSX with expression</span>
html = &lt;div&gt;{name}&lt;/div&gt;;

<span class="hljs-comment"># JSX with control flow</span>
html = &lt;div&gt;{<span class="hljs-keyword">for</span> i <span class="hljs-keyword">in</span> items { &lt;span&gt;{i}&lt;/span&gt; }}&lt;/div&gt;;
