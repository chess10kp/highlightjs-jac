<span class="hljs-comment"># JSX</span>
html = <span class="hljs-tag">&lt;<span class="hljs-name">div</span>&gt;</span>Hello<span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span>;

<span class="hljs-comment"># JSX with attributes</span>
html = <span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-name">class</span>=<span class="hljs-string">&quot;foo&quot;</span>&gt;</span>Hello<span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span>;

<span class="hljs-comment"># JSX with self-closing</span>
html = <span class="hljs-tag">&lt;<span class="hljs-name">img</span> /&gt;</span>;

<span class="hljs-comment"># JSX with fragment</span>
html = <span class="hljs-tag">&lt;&gt;</span>Hello<span class="hljs-tag">&lt;/&gt;</span>;

<span class="hljs-comment"># JSX with expression</span>
html = <span class="hljs-tag">&lt;<span class="hljs-name">div</span>&gt;</span>{name}<span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span>;

<span class="hljs-comment"># JSX with control flow</span>
html = <span class="hljs-tag">&lt;<span class="hljs-name">div</span>&gt;</span>{<span class="hljs-keyword">for</span> i <span class="hljs-keyword">in</span> items { <span class="hljs-tag">&lt;<span class="hljs-name">span</span>&gt;</span>{i}<span class="hljs-tag">&lt;/<span class="hljs-name">span</span>&gt;</span> }}<span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span>;
