<span class="hljs-comment"># Pipe operator in f-string substitution</span>
x = <span class="hljs-string">f&quot;<span class="hljs-subst">{a <span class="hljs-operator">|&gt;</span> b}</span>&quot;</span>;

<span class="hljs-comment"># Pipe back in f-string substitution</span>
y = <span class="hljs-string">f&quot;<span class="hljs-subst">{a <span class="hljs-operator">&lt;|</span> b}</span>&quot;</span>;

<span class="hljs-comment"># Chained pipes in f-string substitution</span>
z = <span class="hljs-string">f&quot;<span class="hljs-subst">{a <span class="hljs-operator">|&gt;</span> b <span class="hljs-operator">|&gt;</span> c}</span>&quot;</span>;
