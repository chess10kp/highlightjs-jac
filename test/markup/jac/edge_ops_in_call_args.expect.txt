<span class="hljs-comment"># Edge operator in function call</span>
foo(a <span class="hljs-operator">--&gt;</span> b);

<span class="hljs-comment"># Edge ref chain in function call</span>
bar(a <span class="hljs-operator">++&gt;</span> b <span class="hljs-operator">&lt;--</span> c);

<span class="hljs-comment"># Edge operator with connect</span>
<span class="hljs-keyword">global</span>(a <span class="hljs-operator">--&gt;</span> b, c <span class="hljs-operator">++&gt;</span> d);
